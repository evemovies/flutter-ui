import 'package:flutter/cupertino.dart';

class AddMovieTab extends StatelessWidget {
  const AddMovieTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Add new movie here'),
    );
  }
}
